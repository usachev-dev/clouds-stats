{
  "name": "clouds-stats",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "bun --watch serve.ts",
    "styles": "bun run build:styles:watch",
    "calculate": "bun ./calculate.ts",
    "clean:dist": "bun run ./scripts/clean-dist.ts",
    "build": "bun run ./scripts/clean-dist.ts && bun run build:assets && bun run build:exe",
    "build:assets": "bun run build:styles && bun run ./scripts/copy-public.ts",
    "build:styles": "bun run ./scripts/build-styles.ts",
    "build:styles:watch": "bun run ./scripts/build-styles.ts dev",
    "build:exe": "bun build --compile ./serve.ts --outfile ./dist/clouds-stats.exe",
    "start": "bun calculate.ts && bun serve.ts"
  },
  "devDependencies": {
    "bun-types": "1.3.10",
    "sass": "1.97.3"
  },
  "dependencies": {
    "css-normalize": "0.0.1",
    "mustache-bun": "^4.2.2",
    "simple-statistics": "^7.8.9",
    "skeleton.css": "^2.0.4"
  }
}
